import { ChangedomDirective } from './changedom.directive';

describe('ChangedomDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangedomDirective();
    expect(directive).toBeTruthy();
  });
});
